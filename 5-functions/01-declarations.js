/* 

      FUNCTIONS

      -functions are a chunk of code that perform a set or set of actions when called (or invoked)
      -functions are reusable- we call them as many times as we want and the can take different sets of data*/

//function declaration example
      function hi(){
          console.log('hello!');
      }

      hi(); //function must be called by its name if we want the function to run